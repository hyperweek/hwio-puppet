<%= logfile %> {
  weekly
  missingok
  rotate 520
  compress
  delaycompress
  notifempty
  create 640 <%= owner %> <%= group %>
  postrotate
    /usr/bin/supervisorctl restart <% name %>-web
  endscript
}
